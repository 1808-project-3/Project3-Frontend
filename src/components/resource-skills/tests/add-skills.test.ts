// import * as React from 'react';
// import {shallow} from 'enzyme';
import AddSkillsComponent from '../add-skills.component';

describe( AddSkillsComponent, () => {
    it('should submit the form', () => {
      //  const wrapper = shallow(AddSkillsComponent);
    });
});