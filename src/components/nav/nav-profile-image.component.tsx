import * as React from 'react';
import "./navStyles.css";

export const NavProfileImage = (props: any) => {
    return(
        <div className="profileImage">
            <img/>
        </div>
    )
}